let numbers = [1, 2, 3, 4];
let another = numbers;

// Uncomment below each solutions

// Solution:: If numbers is let
// numbers = [];

// Solution 2
// numbers.length = 0;

// Solution 3
// numbers.splice(0, numbers.length);

// Solution 4:: Not recommened
// while (numbers.length > 0) numbers.pop();

console.log(numbers);
console.log(another);
